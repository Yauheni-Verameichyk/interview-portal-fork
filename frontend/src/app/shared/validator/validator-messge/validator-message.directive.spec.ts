import { ValidatorMessageDirective } from '/validator-messge/validator-message.directive.ts';

describe('ValidatorMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatorMessageDirective();
    expect(directive).toBeTruthy();
  });
});
