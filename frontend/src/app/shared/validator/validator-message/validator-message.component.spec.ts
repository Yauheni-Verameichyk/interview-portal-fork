import { ValidatorMessageComponent } from "./validator-message.component";


describe('ValidatorMessageComponent', () => {
  it('should create an instance', () => {
    const component = new ValidatorMessageComponent();
    expect(component).toBeTruthy();
  });
});
